## 1. Phase 0：Preflight 基线确认

- [x] 1.1 运行 `git status`、`alembic heads`（预期 `0002_multi_user_auth`，若已变化则以实际 head 为准）、全量 `pytest` 建立绿色基线——结果：head=0002_multi_user_auth；308 passed, 1 deselected；git 工作区含用户自行移入 docs/ 的文档整理（未提交，不属于本变更）
- [x] 1.2 通读现有实现并确认复用点：`app/providers/base.py`、`app/providers/quote/__init__.py`（QuoteProviderRegistry 模式）、`app/observability/provider_metrics.py`（ProviderMetricsRegistry/call_with_metrics）、`app/providers/fundamental/tushare.py`、`app/providers/trading_calendar/provider.py`、`app/db.py`（WriteCoordinator 接口）、现有 jobs 与 lifespan、Admin 路由认证依赖、前端 CSRF 调用方式、`instrument`/`trading_calendar` 模型、tests 组织与 fixture
- [x] 1.3 核对技术方案 §58 目录建议与当前仓库实际命名的差异，确定最终文件清单（允许微调路径，职责不得混乱）——结论：§58 布局与仓库一致，唯一补充为四张 Core 事实表定义放 `app/models/history_fact.py`（注册进 Base.metadata 以纳入防漂移测试）；`app/schemas/history_admin.py` 为首个按域拆分的 schema 文件

## 2. Phase 1：数据库 schema

- [x] 2.1 基于实际 head 创建迁移 `0003_a_share_historical_data`：`trading_calendar` 增加 exchange/pretrade_date/source/fetched_at 四个可空列（简单 ALTER）
- [x] 2.2 迁移创建主档表 `cn_stock_basic`、`cn_stock_company`、`cn_stock_name_change`（ORM 模型放 `app/models/history_market.py`，字段按技术方案 §8~§10）
- [x] 2.3 迁移创建同步控制表 `history_sync_state`、`history_day_status`、`history_sync_run`、`history_sync_run_dataset`（ORM 模型放 `app/models/history_sync.py`，含 dataset/dataset_kind/status/trigger_type/run status 常量枚举，dataset 名称固定为代码常量）
- [x] 2.4 迁移创建四张日级事实表 `market_daily_bar`、`market_adj_factor`、`market_daily_basic`、`market_moneyflow`（SQLAlchemy Core Table，无物理主键/外键/索引，字段与类型按技术方案 §13~§16）——Core Table 定义放 `app/models/history_fact.py` 并注册进 Base.metadata
- [x] 2.5 迁移测试：从 0002 状态库（预置 instrument/fundamental_snapshot/watchlist/user/trading_calendar/job_status 数据）升级到 0003，验证旧数据行数与内容不变、新表存在可查询、trading_calendar 新列为 NULL、模型 metadata 与迁移产物一致
- [x] 2.6 全量 `pytest` 通过——结果：314 passed, 1 deselected（基线 308 + 新增 6 个迁移测试）

## 3. Phase 2：Provider 框架扩展（不接调度）

- [x] 3.1 在 `app/providers/base.py` 增加历史内部标准模型（StockBasicRecord/StockCompanyRecord/StockNameChangeRecord/DailyBar/AdjFactor/DailyBasic/MoneyFlow）、`ProviderBatch[T]`（records/source/raw_row_count/truncation_risk）与 `HistoricalMarketDataProvider` Protocol；确认不含 Tushare SDK 类型
- [x] 3.2 `app/config.py` 与 `config.example.yaml` 增加 `providers.history` 选源与 `history.*` 配置节（enabled/start_date/schedule_time/startup_catchup/max_attempts/request_min_interval_seconds/backoff_*/jitter_ratio/stock_basic_refresh_hours/master_refresh_days/availability.*），缺省默认值可启动
- [x] 3.3 新增 `app/providers/tushare_common.py`：`create_tushare_pro_client(config)` 与 `TushareRequestGate`（threading.Lock + time.monotonic()，按 endpoint 最小间隔，默认 0.6s、stock_basic ≥1.25s，可配置）——含 `TushareTransport`（懒建 client + SDK 异常归一化为带 error_code 的 TushareError 子类）与进程级共享 gate（`configure_shared_gate`/`get_shared_gate`，main.py 启动时按 config.history 注入间隔）
- [x] 3.4 改造现有 `TushareFundamentalProvider` 与 `TushareTradingCalendarProvider` 共享 `tushare_common` transport（业务契约与页面行为不变，现有测试全量通过）——`_pro()` 返回 `TushareClientProxy`（endpoint 转发 transport.call，保留测试 mock 接缝）；calendar 经 transport 补上 timeout；main.py lifespan 以 `configure_shared_gate(build_gate_from_config(config))` 初始化进程级 gate；17 个既有单测全过
- [x] 3.5 新增 `app/providers/history/__init__.py` 的 `HistoryProviderRegistry`（仿 QuoteProviderRegistry：AppConfig 选源、单例、call_with_metrics 包装、注入 providers.timeout.tushare、tushare_history_* metrics key）——metrics key `{source}_history_{dataset}`（fallback 方法计入同一 dataset）；另提供可选 `provider` 显式注入参数（生产按配置构造单例不变，测试免 monkeypatch）
- [x] 3.6 实现 `app/providers/history/tushare.py` 的 `TushareHistoricalMarketDataProvider`：每个数据集定义 EXPECTED/REQUIRED/OPTIONAL 字段常量并显式传 fields；Tushare 返回结构校验、日期/代码解析、ts_code→instrument_id 映射（基于 cn_stock_basic，不按代码首位推断）、数值转换、截断上限识别（6000/4500）、normalize 为内部模型并返回 ProviderBatch；异常向 Service 传播不吞掉——REQUIRED 身份字段缺列/空值抛 SCHEMA_MISMATCH；未知 ts_code 抛 UNKNOWN_INSTRUMENT；0 行返回空 batch（空结果语义由 Service 判定 §34）；主档 instrument_id 按 §7.1 规则 CN:STOCK:<symbol> 生成
- [x] 3.7 实现分片与 fallback：stock_basic 按 exchange(SSE/SZSE/BSE)×list_status(L/D/P/G/UN) 分片（空分片允许、命中上限则整轮失败）、stock_company 按 exchange 分片、日级数据集 6000 行触发更细粒度 fallback（对未验证多代码参数的接口保守逐证券）——分片命中上限置 truncation_risk 交 Service/Validator 拒绝；`*_for_instruments` 按 Instrument.exchange 显式映射 ts_code 后缀（SH/SZ/BJ，不按代码首位推断），逐证券合并不去重（重复键检测属 Domain 校验）；adj_factor 无文档上限按同一 6000 阈值保守防护（§33.5）
- [x] 3.8 扩展 `TushareTradingCalendarProvider`：新增 strict 范围读取（`get_days(market, start, end, strict=True)`——必须来自 Tushare、无 weekday fallback、缺数据抛异常、返回/缓存 pretrade_date 与 source），strict=False 行为与现有完全一致——`CalendarDayRecord` dataclass + Repository 扩展（`save_days_strict` upsert 覆盖旧行元数据、`has_strict_year` 按 source='tushare' 判定、`get_days_between`）；严格路径按整年拉取并要求全年逐日完整（缺失即 `CalendarUnavailableError`，绝不 weekday 补造）；网络在写锁外、落库持锁（D6）；trade_cal 显式 fields 含 pretrade_date
- [x] 3.9 Provider 单元测试（全离线 mock）：字段映射、normalize、截断标志、未知 ts_code、分片参数、strict 日历无降级——`tests/unit/test_history_provider.py`（36 测试：字段常量口径、daily/adj_factor/daily_basic/moneyflow normalize 与 NULL 保留、0 行空 batch、SCHEMA_MISMATCH/UNKNOWN_INSTRUMENT、6000/4500 截断、15+3 分片参数、namechange 参数格式化、fallback 逐证券 exchange 后缀、Registry metrics key/超时分类/选源报错）+ `tests/unit/test_trading_calendar_strict.py`（7 测试：拉取落库/缓存命中/旧行覆盖/缺日抛错/空返回/无 Token/strict=False 降级不缓存）
- [x] 3.10 `@pytest.mark.online` smoke tests（不写生产库）：stock_basic 一个 shard、trade_cal 小窗口、最近一个已完成交易日的 daily/adj_factor/daily_basic/moneyflow、company 一个 exchange、namechange 一只已知证券；验证权限、字段、fields 参数、日期格式、BSE ts_code 格式、上限行为，并记录 namechange 规范键唯一性观察结论（供 Open Question 决策）——用例已全部写好（9 个，覆盖上述各项）。**2026-09-19 已用真实 Token 在线跑通：9 passed**（17.96s，只读外部接口、不写库）。已修复一处标记缺陷：原先 `@pytest.mark.online` 只写在 docstring 里、未作为模块级 `pytestmark` 生效，导致这 9 个用例逃过默认 `-m 'not online'` 过滤（配了 Token 的机器上跑默认 `pytest` 会真实访问网络，违反 §76.1；未配 Token 时表现为"恰好 skip"掩盖了问题）

## 4. Phase 3：Repository + Validator

- [x] 4.1 `app/repositories/history_master.py`：instrument + cn_stock_basic 同事务 upsert（全 shard 内存校验后一次性提交、退市不删除）、cn_stock_company upsert、cn_stock_name_change 整证券替换与重叠窗口增量（保留更早历史）、严格日历的读写（填充 exchange/pretrade_date/source/fetched_at）。实现过程中系统排查并修复了一系列 SQLAlchemy flush 时机问题：跨表外键依赖（Instrument 必须独立先于 CnStockBasic flush）、"先 add 空壳后逐字段赋值"模式下 flush 必须放在全部字段赋值完成之后。
- [x] 4.2 `app/repositories/history_fact.py`：按 dataset 映射 Core 表、查某日行数、DELETE 当日、executemany 批量 INSERT（可 1000~2000 行分 chunk）、old_count 返回；不逐行 ORM。
- [x] 4.3 `app/repositories/history_sync.py`：state 读写与水位推进、day ledger upsert、run/run_dataset 生命周期、stale RUNNING 查询与恢复标记。同步修复了纯 ORM 属性赋值场景（run.status/day_status 更新已有行等）在同事务内后续查询前缺失显式 flush 的问题。
- [x] 4.4 `app/services/history/validation.py`：通用校验（instrument_id/trade_date 非空、日期一致、唯一键去重、非零行、truncation_risk=False、NaN/Inf、instrument 可映射）+ 各数据集专项规则（daily OHLC/vol/amount 与 high/low 关系、ah_* 可 NULL；adj_factor>0 且不重复；daily_basic 股本市值非负、limit_status 枚举、估值允许 NULL；moneyflow 买卖非负、net 可负）。异常类均带 error_code 属性（EMPTY_RESULT/TRUNCATION_RISK/DUPLICATE_KEY/TRADE_DATE_MISMATCH/UNKNOWN_INSTRUMENT/INVALID_VALUE）。
- [x] 4.5 Repository/Validator 集成测试（真实临时 DuckDB + mock 数据）：批量写、整日替换、校验拒绝路径。`tests/integration/test_history_repositories.py`，39 passed。

## 5. Phase 4：HistorySyncService（核心）

- [x] 5.1 `app/services/history/availability.py`：AvailabilityPolicy——按数据集 cutoff（默认 adj_factor 09:30/daily 16:30/daily_basic 17:30/moneyflow 20:30）与 Asia/Shanghai 时间计算 latest_expected_trade_date，时区无关。已实现，临时脚本验证盘中未到 cutoff 回退上一交易日、已过 cutoff 取当日两类场景。
- [x] 5.2 `app/services/history/planner.py`：水位→pending dates（严格日历升序、禁止 date+1）、目标日期、lag 天数、reconcile（扫描 day ledger 找第一个缺口并保守回退水位）。`HistorySyncPlanner` 纯函数实现：`pending_dates`/`lag_days`/`reconcile_watermark`；新增 `tests/unit/test_history_planner.py` 覆盖 availability + planner 全部场景，14 passed。
- [x] 5.3 `app/services/history/sync_service.py`：`run(trigger, requested_by)` 总编排——recover_stale_runs → ensure_master_prerequisites（trade_cal/stock_basic 硬前置、company/namechange 非阻塞）→ reconcile → 四数据集顺序执行互不阻塞 → finalize（SUCCESS/PARTIAL/FAILED/NOOP）。`HistorySyncService.run(requested_by_user_id=...)` 为唯一业务入口（服务端取用户，非客户端传入）；`_finalize_run` 覆盖 SUCCESS/PARTIAL/FAILED/NOOP/INTERRUPTED 五态并按 §20 补写主档 run_dataset 行（含真实 rows_written/request_count）。
- [x] 5.4 实现单日原子提交：请求/校验在写锁外，WriteCoordinator 内单事务完成 [old_count→DELETE→批量 INSERT→day_status→state（水位/record_count/min-max/进度）→run_dataset]，失败整体回滚。`_commit_single_day` 严格按 §22 十一步；`record_count` 增量口径为 `new_count - old_count`（§45，重跑同日不翻倍）；DB 异常转 DATABASE_ERROR 在 10 次边界内重试（§8），不嵌套 WriteCoordinator 自带重试。
- [x] 5.5 实现重试编排：单 dataset×单日最多 10 次、指数退避+抖动（sleep/random 可注入）、配置类错误快速失败、错误码标准化与敏感信息过滤。`retry.py`（CONFIG_ERROR_CODES/is_config_error/RetryPolicy）+ `sync_service._sync_single_day` 重试循环；`SYNC_ERRORS` 统一捕获 TushareError/HistoryValidationError/CalendarUnavailableError/内建 TimeoutError（call_with_metrics 线程级超时抛后者）；`_error_code_of` 归一化错误码，`_safe_error_text` 过滤 Token。
- [x] 5.6 实现 namechange bootstrap（master_cursor 逐只推进可中断续跑）与主档刷新周期（stock_basic 24h、company/namechange 7 天）。`_namechange_bootstrap_step` 锁外逐只请求、锁内整批替换；`is_last_chunk = len(fetched) == len(pending)`（取消时游标停在本批最后一只，不越过未取证券）；`_refresh_stock_basic` 兼作 §35.1 未知证券恢复（刷新一次后重映射，仍未知才失败）；`_exchange_suffix` 抛 UNKNOWN_INSTRUMENT 领域异常而非裸 ValueError（脏 exchange 不得阻塞日级数据集）。
- [x] 5.7 实现 cancellation_event 检查点（每日前/重试 sleep 前后/master 分片间；停机时当前事务完成、不开始下一日）。`run(trigger, ..., cancellation_event=event)` 贯穿 ensure_master_prerequisites → _ensure_namechange → _namechange_bootstrap_step（master 分片间）→ _sync_day_level_dataset（每日前）→ _handle_attempt_failure（sleep 前后）；命中返回 CANCELLED，run 记 INTERRUPTED。
- [x] 5.8 核心集成测试（mock Provider + 真实临时 DuckDB）：首次三天同步（事实/ledger/水位/record_count）、中间失败绝不跳日（01-06 未被请求）、下次从失败日恢复、数据集独立推进、DELETE 后/INSERT 后/state 更新前注入异常的回滚、重复运行不重复、6000 截断 fallback 与失败不推进、空结果（历史 EMPTY_RESULT、当日 WAITING_SOURCE）、stale run 恢复（RUNNING→INTERRUPTED、state 恢复、catch-up 续跑）。`tests/integration/test_history_sync_service.py` 42 测试；另含评审发现的两处非预期异常穿透回归（内建 TimeoutError 重试、reconcile 阶段崩溃不得记 NOOP）、脏 exchange 不阻塞日级、主档 run_dataset 计数非零。
- [x] 5.9 单元测试：availability（交易日上午/收盘后/各 cutoff 前后/周末/节假日/时区无关）、planner（NULL 水位/一年 backlog/无 pending/reconcile 缺口）、retry（第 1/3/10 次成功、10 次失败、delay cap、jitter、不真实 sleep）。`tests/unit/test_history_planner.py` 14 测试（availability + planner）+ `tests/unit/test_history_retry.py` 10 测试（退避序列 5/10/20/40/80/160/300/300/300、抖动区间、sleep 注入、配置类错误分类）。
- [x] 5.10 实现同步日志规范（技术方案 §62/§63）：结构化字段 run_id/dataset/trade_date/attempt/row_count/elapsed_ms/error_code；级别纪律（正常完成 INFO/重试 WARNING/10 次失败 ERROR）；回填期间每交易日一条概要、不逐行打印；日志与错误文本不含 Token。`_elapsed_ms` 计时、单日完成 INFO 概要一条（含 row_count）、`_handle_attempt_failure` WARNING（attempt+error_code）、达上限 ERROR；`_safe_error_text` 正则脱敏 token 并截断 500 字符，日志与 state 均用其输出（测试断言明文 Token 不出现）。

## 6. Phase 5：HistorySyncJob

- [x] 6.1 新增 `app/jobs/history_sync.py`：start/stop、每日 schedule_time（默认 20:30 Asia/Shanghai，含周末）调度循环、startup catch-up（发现落后即触发 STARTUP）、`asyncio.to_thread` 执行同步 Service、进程级 single-flight（运行中定时记 skip、手动 409 由 API 层返回）、shutdown cancellation event 管理、JobStatusService 接入（job_name `history_sync`，仅高层健康）。实现：`_parse_schedule_time` 解析 `HH:MM`（非法配置抛 ValueError 不静默降级）；`_run` 循环每 30s 判定 `_should_fire_scheduled`（当天未触发过且落在 `[schedule_time, +5min)` 容差窗口内，窗口外不补跑历史时点——缺口交给 startup catch-up 与业务水位）；`run_scheduled`（失败记 WARNING 跳过，返回 None）与 `try_begin`/`run_manual`（API 层据此回 409）共用同一把 `threading.Lock` 做进程级 single-flight，各自 `try/finally` 释放；`run_manual` 的 `requested_by_user_id` 由服务端传入；`_execute` 经 `asyncio.to_thread` 调 `HistorySyncService.run`，兜底异常并记 JobStatusService，绝不让调度循环退出；`stop()` 先置 cancellation event（Service 在当前事务边界收尾）再取消 task；`running_run_id()` 取库里唯一 RUNNING 行供 409 响应体。
- [x] 6.2 接入 FastAPI lifespan（遵循现有 Job 注册方式），`history.enabled=false` 时不启动。实现：`create_app` 的 lifespan 中在 `fundamental_job` 之后构造 `HistoryProviderRegistry` + `HistorySyncService` + `HistorySyncJob`，挂载 `app.state.history_sync_service` 与 `app.state.history_sync_job`；`config.history.enabled` 为 false 时二者均不构造（`history_sync_job` 为 None）；`await history_job.start()` 与 finally 中 `await history_job.stop()` 与既有 Job 同批。
- [x] 6.3 Job 测试：定时触发与 skip、startup catchup、single-flight 并发只有一个 run、优雅停机。实现：`tests/unit/test_history_sync_job.py` 23 例（Service 用 stub 替身，不触库）：`TestScheduleTimeParsing`（合法/非法参数化/默认 20:30）、`TestShouldFireScheduled`（整点触发、容差内、到点前、远超窗口不触发、当日仅一次、周末运行）、`TestSingleFlight`（并发只有一次 run、结束后可再跑、`is_running` 反映状态、`try_begin` 第二次失败=409、`run_manual` 透传服务端 user_id）、`TestStartupCatchup`（开关生效/关闭不触发）、`TestGracefulShutdown`（stop 置 cancellation event、未 start 也安全）、`TestJobStatusIntegration`（started/success 记录、异常记 failure 且释放 single-flight）；另加 `tests/integration/test_history_sync_job_lifespan.py` 3 例验证 6.2 接线：enabled 时挂载并启动、disabled 时为 None、退出 lifespan 后调度 task 清理。

## 7. Phase 6：Admin API

- [x] 7.1 `app/schemas/history_admin.py`（现有 Pydantic Schema 集中于 `app/schemas/__init__.py`，此为首个按域拆分，属允许的组织微调）：summary（overall_status/active_run/daily_datasets/master_datasets 全字段）、sync 响应（202/409）、runs 列表与详情 Pydantic 模型，时间字段北京时间带时区 ISO。实现：`DailyDatasetSummary` 覆盖 §52.1 列出的全部 16 字段（dataset/display_name/status/history_start_date/data_min/data_max/latest_complete/latest_expected/next_trade_date/lag_trade_days/record_count/current_trade_date/current_attempt/last_success_at/last_error_code/last_error）；`MasterDatasetSummary` 只含状态/记录数/上次成功/cursor/bootstrap/最后错误——主档无交易日水位语义，不提供伪造字段；`ActiveRunSummary`/`HistorySummaryResponse`/`SyncStartResponse`(202)/`SyncConflictResponse`(409)/`RunItem`/`RunDatasetDetail`/`RunListResponse`/`RunDetailResponse`；运行时字段缺省为 null 而非缺键。
- [x] 7.2 `app/api/admin_history.py`：`GET /api/admin/history-data/summary`（仅管理员、只读 sync 小表）、`POST /api/admin/history-data/sync`（管理员 + 现有 CSRF、202/409、requested_by 服务端取）、`GET /api/admin/history-data/runs?limit=20`、`GET /api/admin/history-data/runs/{run_id}`；注册到应用路由。实现：Router 层 `dependencies=[Depends(require_admin)]`（未登录 401、普通用户 403，写请求由既有 CsrfMiddleware 校验）；summary 只读 `history_sync_state` + `history_sync_run` + 已缓存严格日历（source='tushare'）——GET 不触发任何网络请求，日历未缓存时目标日期退回 state 表已记录值；`overall_status` 按 §84 优先级（任何 active run → RUNNING；核心日级 FAILED 或 trade_cal/stock_basic FAILED → ERROR；落后且非全为 WAITING_SOURCE → LAGGING；仅等待数据源 → WAITING；四个全 CAUGHT_UP → HEALTHY；全 UNINITIALIZED → UNINITIALIZED）；`next_trade_date`/`lag_trade_days` 复用 `HistorySyncPlanner` 纯函数；POST 用 `job.try_begin()` 判定 409（附 `job.running_run_id()`），成功则预留 `run_id` 并以 `asyncio.create_task(asyncio.to_thread(job.run_manual, current_user.user_id, run_id=run_id))` 后台执行、立即回 202（HTTP 不等待回填）；`requested_by_user_id` 取自 `CurrentUser`，请求体传值被忽略；后台任务用模块级 `_BACKGROUND_TASKS` 强引用防 GC。`HistorySyncService.run` 与 `HistorySyncJob.run_manual/_execute` 相应新增可选预留 `run_id` 形参（默认仍为自生成 UUID）。
- [x] 7.3 API 测试：未登录 401/普通用户 403/管理员 200、CSRF 缺失拒绝、运行中 409、run 详情进度字段、summary 不触发事实表扫描。实现：`tests/integration/test_admin_history_api.py` 25 例——`TestAuthAndCsrf`（summary/runs/runs/{id}/sync 的四类权限 + 缺失与错误 X-CSRF-Token 均 403 且不触发同步）、`TestSummary`（全新库键齐全且主档无伪造水位字段、日级字段集合与 §52.1 逐一对齐、失败数据集水位不越过失败日且 next_trade_date 指向失败日、主档 cursor/bootstrap、overall_status 的 RUNNING/HEALTHY/核心主档失败升级 ERROR/非核心主档失败不升级、`before_cursor_execute` 事件断言 SQL 未触碰四张事实表）、`TestManualSync`（202 回传预留 run_id、409 附运行中 run_id 且不启动第二个 run、`requested_by_user_id` 用认证用户 id 忽略请求体传值、未启用 503）、`TestRuns`（列表/详情字段含 dates_completed/rows_written/retry_count/起止水位、404、limit 参数校验）、`TestNoTokenLeak`。配套：`tests/conftest.py` 的 `client_factory` 默认关闭 `history.startup_catchup`（否则每个 API 测试的 lifespan 都会真发一次 STARTUP run，污染 history_sync_* 断言）。

## 8. Phase 7：Admin 页面

- [x] 8.1 `app/templates/admin_data.html`：顶部总体卡（整体状态/数据起点/最新交易日/当前任务/最后执行 + "检查并更新数据"按钮）、四个日级数据集状态卡、主档状态表、当前任务进度、最近 20 次执行记录；复用 `require_admin_page` 与现有 CSRF meta、保持现有 UI 风格。实现：`<body data-page="admin-data">`；总体卡五个 overview-item + `#sync-button`；`#daily-cards`（JS 渲染四卡）、`#master-table`、`#active-run`（当前任务进度）、`#runs-table` + `#runs-empty`；`<meta name="csrf-token">` 注入；页脚说明首次回填起点与限流预期；无任何表单输入（不提供历史数据手工编辑/水位输入入口）；样式复用 `.panel/.table/.muted` 并在 style.css 追加 `.data-overview/.status-badge(ok|busy|warn|bad)/.data-card` 等本页专用类。
- [x] 8.2 原生 JS（`initAdminDataPage()` 或独立 admin_data.js，无构建系统）：渲染 summary、运行中每 3~5 秒轮询 summary/active run、任务结束停止轮询、POST sync 携带 X-CSRF-Token、运行中按钮禁用显示"正在更新..."。实现：`app/static/app.js` 新增 `initAdminDataPage()`（并入既有单文件、DOMContentLoaded 按 `data-page` 分派，不引入构建系统）：`DATASET_STATUS_*`/`OVERALL_STATUS_*`/`TRIGGER_LABEL`/`RUN_STATUS_LABEL` 中文映射；`refresh()` 拉 summary → 渲染总体卡/四卡/主档表 → 有 active_run 时再拉 `runs/{run_id}` 渲染进度表并 `startPolling()`，无则 `stopPolling()` 并显示"当前没有运行中的同步任务"；轮询间隔 4 秒（3~5 秒区间内），失败即停止轮询并提示；`beforeunload` 清理定时器；`syncBtn` 点击用既有 `api()` 封装（自动带 X-CSRF-Token），运行中禁用并显示"正在更新..."，409 时提示"历史数据同步正在运行"并继续轮询；最近 20 条执行记录逐条取详情渲染各数据集推进 chips 与耗时（`fmtDuration`）；所有动态文本经 `esc()` 转义。
- [x] 8.3 管理员导航入口接入 `/admin/data`（现有导航为各页面模板内联、无 base 模板，需在 index/watchlist/tags/change_password/admin_users/admin_status 逐个加入）。实现：`index/watchlist/tags/change_password` 的管理员条件块内加 `<a href="/admin/data">数据管理</a>`（置于"用户管理"前）；`admin_users.html`、`admin_status.html` 的无条件管理员导航同样加入；`admin_data.html` 自身提供指向用户管理/系统状态/首页的链接；`app/main.py` 新增 `GET /admin/data`（`Depends(require_admin_page)`，未登录按初始化状态 302 → /setup 或 /login，普通用户 403）。
- [x] 8.4 页面级测试：管理员可见、普通用户 403、未登录 302 跳转登录页（沿用现有管理员页面行为，空库跳 /setup）、运行中状态渲染。实现：`tests/integration/test_admin_data_page.py` 15 例——空库匿名 302 `/setup`、已初始化库匿名 302 `/login`（401 语义仅属 API）、普通用户 403 且不含页面内容、管理员 200 且九个 DOM 锚点齐全、单一主操作按钮且无表单输入（不提供手工编辑/水位入口）、CSRF meta 存在、本页导航链接、参数化六个页面（index/watchlist/tags/change-password/admin/users/admin/status）均含数据管理入口且普通用户不可见、静态资源引用。运行中状态的服务端渲染契约由 `test_admin_history_api.py::TestRuns`（详情进度字段）与 `TestSummary`（active_run/overall_status=RUNNING）覆盖，前端据此渲染。

## 9. Phase 8：回归、文档与发布（v0.3.0）

- [x] 9.1 全量离线 `pytest` 通过（不访问真实网络）——**548 passed / 10 deselected**（490 s，无 failed 无 skipped）。本轮修复三处：① `test_conflict_reports_reserved_run_id_immediately` 竞态偶发失败（测试先等后台线程复位 busy，再模拟"已启动未落库"）；② `test_history_online_smoke.py` 的 9 个用例原先只把 `@pytest.mark.online` 写在 docstring 里、未作为模块级 `pytestmark` 生效，**逃过了默认 `-m 'not online'` 过滤**（配 Token 的机器上跑默认 `pytest` 会真实访问 Tushare，违反 §76.1；未配 Token 时表现为"恰好 skip"掩盖问题）——补 `pytestmark` 后由 runtime-skip 变为 collection-time deselected；③ 陈旧断言 `test_admin_status_structure` 的 jobs 集合（history_sync 为新增第三个 Job）
- [x] 9.2 `pytest -m online` smoke 通过（真实 Tushare、不写生产库），并将 namechange 键唯一性与多代码参数能力观察结论回填 design.md Open Questions——2026-09-19 实跑 **9 passed**；关键观察：`daily` 5553 / `adj_factor` 5565 / `daily_basic` 5553 / `moneyflow` 5553 行（距 6000 上限仅 435~447 行，约 8% 余量），`stock_basic` SSE/L 2320（上限 6000）、`stock_company` SSE 2457（上限 4500），BSE 当日 344 行，`namechange 000001.SZ` 4 条事件且规范键 `(ts_code,name,start_date)` 重复 0 条、`end_date`/`ann_date` 非空率 4/4。
- [x] 9.3 本地 synthetic 性能基准：约 6000 行 × 100 交易日的整日 delete+insert+state 更新，确认无逐行 ORM/每行 commit 退化——`scripts/bench/bench_history_write.py` 三项断言全 PASS（提交次数 200 = 2×100；SQL 语句 1731 条 << 1200000 行；批量 0.099 ms/行，比同机逐行基线快 48.2×）。据此按 design.md 风险条目替换 Repository 批量写入为"注册 DuckDB 视图 + INSERT SELECT"：中位单日替换 4924.6 ms → 1182.9 ms，总耗时 497 s → 124 s
- [x] 9.4 文档与配置：`config.example.yaml` history 节（13 个字段与 `AppConfig().history` 默认值逐一核对一致）、README（历史数据功能说明、/admin/data、首次回填跨多次运行预期、限流策略、升级前备份 DuckDB、`history.*` 配置）、CHANGELOG（补充批量写入实现变更）、版本号升级 v0.3.0（`app/version.py` + `pyproject.toml` 同步；`pandas>=2.0` 提为显式运行时依赖）
- [x] 9.5 发布前核对技术方案 §86 Definition of Done 清单与 §76.1/76.2 硬约束（不新建平行 Provider 框架、不删 FundamentalRefreshJob、不改 fundamental_snapshot、不引入 Redis/Celery、无逐行 ORM、无 Token 泄漏路径等）——§86 六个板块逐项核对通过（数据库 5/5、Provider 7/7、Job 5/5、Admin 5/5、Tests 6/6，同步项由既有集成测试覆盖）；§76.1 逐条排查：无 `providers/history/base.py`、无 Celery/Redis、Dockerfile 仍 `--workers 1`、无第二 trade_cal Provider、无历史专属 metrics/timeout、`fundamental_snapshot` 仅出现在"不动它"的约束注释与行数校验清单中、历史路径无工作日 fallback、无 qfq/hfq 表、管理员页面与 API 不出现 Token 值、事实表无逐行 ORM insert；CSRF 无新增豁免路径（`EXEMPT_PATHS` 仅登录/初始化两条既有项，且有 403 运行时测试）
- [x] 9.7 发布前收尾修复：`finish_success` 清除陈旧错误字段（回归用例 `test_admin_history_api.py::TestSummary::test_failed_then_success_no_longer_shows_stale_error`，突变验证：还原旧实现后 state 层与 summary 层断言各自独立失败；修复后全量离线 **563 passed / 10 deselected**，597 s）。现场依据：真实同步第一轮 `real_sync.log:114` 出现 `stock_basic status=CAUGHT_UP ... err=TUSHARE_TIMEOUT` ——同一行内状态与错误码自相矛盾，管理员页面会把上一轮失败的错误码当"最后错误"持续展示。清除集含 `last_error_code`/`last_error`/`last_error_at` 与 `current_trade_date`/`current_attempt`；历史错误由 `history_sync_run_dataset`（含 `finished_at`）保留，不丢失。注意与 `job_status` 表语义相反（那张表按 job-status spec 刻意在成功时保留 `last_error`），勿一并"修正"。
- [x] 9.8 发布前只读核验（用户指定，均无需改码）：① daily_basic 截断 fallback 逐只请求集合确认为 `sorted(candidate_ts_codes − returned_ts_codes)`（`sync_service.py:1202-1207` → `tushare.py:708-715`），已在 Provider 层实测 `missing=None/[]/["600519.SH"]` 分别发 2/0/1 次请求，空列表与 None 严格区分（`is None` 判断）；触发条件仅 `DAILY_ROW_CAP=6000`（无配置阈值）。② `NAMECHANGE_BOOTSTRAP_CHUNK_SIZE=50` 为 `sync_service.py:87` 单一集中常量，运行时唯一引用 `:565`，无重复定义与散落魔法数字，当前不可经 config.yaml 配置（属现状，非缺陷）。
- [x] 9.6 真实数据部署演练：停服备份 `data/marketmind.duckdb` → 更新代码/配置 → `alembic upgrade head` → 启动 → `/admin/data` 触发首次同步并观察进度（可在部署环境执行或形成操作手册）——已在真实数据副本上完整走通并形成操作手册 `docs/部署手册_v0.3.0.md`：0001 副本 `alembic upgrade head` 成功（既有 10 张表行数逐表比对一致、新增表与 4 个可空列到位）、单 worker 启动、`/health` / 权限 302/401 / CSRF 403 / `POST sync` 202 / 并发 409 带 run_id / `GET runs` / 优雅停机全部通过；原始库未被修改（操作前后 md5 一致）。真实 Tushare 首次回填仍待 Token（见 3.10/9.2）
