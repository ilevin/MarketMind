## 1. 初始化状态与用户服务

- [x] 1.1 在用户仓储/服务中实现统一的未初始化状态判定：识别空用户表，以及仅含迁移 `PLACEHOLDER_HASH` admin 且无其他用户的升级库
- [x] 1.2 实现首用户创建/占位管理员认领服务方法：复用用户名与密码校验，锁外计算 Argon2id 哈希，写锁内二次检查并在同一事务提交，保证并发请求至多成功一次
- [x] 1.3 为首用户认领补充用户名冲突、真实用户已存在、占位账户状态异常和数据库失败的错误映射；确保失败不留下半成品用户或修改既有用户数据
- [x] 1.4 为成功初始化补充 Session 创建所需的服务调用，确保首用户固定为启用的 admin，并保留升级库 legacy owner 的 user_id 与私有数据

## 2. 认证 API 与访问控制

- [x] 2.1 新增 `POST /api/auth/setup` 请求/响应 schema 和路由：仅接收用户名、密码、确认密码，不接受 role、is_active、user_id 等权限字段
- [x] 2.2 为 setup 接口实现 422 校验、已初始化 409、并发失败 409 和成功 200 响应；成功时下发与登录一致的 HttpOnly/SameSite/Secure/Path Session Cookie
- [x] 2.3 将 setup 接口加入 CSRF 匿名豁免路径，并增加按 IP 的进程内限速；检查日志路径不得输出密码、哈希或 Session/CSRF Token
- [x] 2.4 增加认证初始化状态依赖/辅助函数：未初始化页面访问重定向 `/setup`，已初始化页面继续重定向 `/login`，私有 API 保持 401 语义
- [x] 2.5 修改 `/login` 与 `/setup` 页面路由，使初始化完成后 setup 不再展示可提交表单，且已有真实用户时不再进入初始化流程

## 3. 页面与前端交互

- [x] 3.1 新增首次访问 setup Jinja2 模板，提供用户名、密码、确认密码字段、密码规则提示和通用错误区域
- [x] 3.2 在前端脚本中实现 setup 表单提交、重复提交按钮禁用、成功后的首页跳转以及 422/409/429 错误展示
- [x] 3.3 确保未初始化访问首页、登录页和 setup 页的跳转行为与标准 Session 登录后的页面行为一致，并覆盖匿名资源访问不受影响

## 4. CLI、部署文档与兼容性

- [x] 4.1 保留并验证 `users set-password/create/promote` 后备路径，补充首访初始化完成后 CLI 创建普通用户的兼容行为
- [x] 4.2 更新 README 的全新部署步骤：启动服务后访问 `/setup` 创建第一个管理员，移除必须先手动设置管理员密码的全新部署前置步骤
- [x] 4.3 更新 README 的 v0.1.0 升级说明：在受控网络通过 `/setup` 认领占位管理员，保留停服后 CLI 设置密码的后备路径及 DuckDB 单写者约束
- [x] 4.4 更新 Docker/本地部署相关说明与 OpenSpec 主规格引用，明确迁移先行、单 worker 和初始化前后访问流程不变

## 5. 测试与验收

- [x] 5.1 增加空用户库首次访问与 setup 成功集成测试：页面跳转、首用户 admin 属性、Session Cookie、首页和管理员 API 访问
- [x] 5.2 增加 setup 输入校验和安全边界测试：非法用户名、弱密码、密码不一致、额外权限字段、CSRF 豁免、限速和错误信息不泄露敏感值
- [x] 5.3 增加初始化完成后拒绝重复 setup、已有真实用户跳转登录、匿名私有 API 仍返回 401、普通用户仍无法访问 admin API 的测试
- [x] 5.4 增加迁移占位管理员认领测试，验证原 user_id、watchlist/index_watchlist/tag/watchlist_tag 数据保持不变且可登录
- [x] 5.5 增加并发 setup 测试，验证至多一个成功、失败请求返回 409 且用户表不存在第二个首用户
- [x] 5.6 运行完整测试套件与 OpenSpec 校验，确认现有多用户认证、数据隔离、迁移、CLI 和 CSRF 测试无回归
